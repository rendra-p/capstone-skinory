<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkinOry"
        tools:targetApi="31">
        <receiver android:name=".ui.notifications.notify.NotificationReceiver" />

        <activity
            android:name=".ui.notifications.chose.SelectProductActivity"
            android:exported="false" />
        <activity
            android:name=".ui.notifications.night.NotifNightActivity"
            android:exported="false" />
        <activity
            android:name=".ui.notifications.day.NotifDayActivity"
            android:exported="false" />
        <activity
            android:name=".ui.notifications.chose.ChoseActivity"
            android:exported="false" />
        <activity
            android:name=".ui.analysis.AnalysisActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>